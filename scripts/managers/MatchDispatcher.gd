# scripts/managers/MatchDispatcher.gd
# RETIRED — match execution now lives in GameManager.advance_week().
# Kept as an empty stub to avoid UID orphan errors.
class_name MatchDispatcher
extends RefCounted
